<%= form_for(@movies2) do |f| %>
  <% if @movies2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movies2.errors.count, "error") %> prohibited this movies2 from being saved:</h2>

      <ul>
      <% @movies2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :genre_id %><br>
    <%= f.collection_select :genre_id, Genre.all, :id, :name  %>
  </div>
  <div class="field">
    <%= f.label :actor_id %><br>
    <%= f.collection_select :actor_id, Actor.all, :id, :name  %>
  </div>
	<div class="field">
	<%= f.label :rating %><br>
	<%= f.text_area :rating %>
  </div>
	<div class="field">
		<%= f.label :poster %><br>
		<%= f.text_area :poster %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
