<h1>Listing movies2s</h1>
<!-- <!--  added to show poster img -->
<!--<div class="row">
	<% @movies2s.each do |movies2| %>
	<div class="col-sm-6 col-md-4">
		<div class="thumbnail">
			<img src="<%= movies2.poster %>">
			<div class="caption">
				
				<%= link_to "More Info", movies2, class: "btn btn-default" %>
			</div>
		</div>
	</div>
	<% end %>
</div>
	<br>
<%= link_to 'New Movies2', new_movies2_path, class: "btn btn-primary" %> -->
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Genre</th>
      <th>Actor</th>
			<th>Poster</th> 
			<th>Rating</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies2s.each do |movies2| %>
      <tr>
        <td><%= movies2.name %></td>
        <td><%= movies2.description %></td>
				<td><%= movies2.genre.name %></td>
				<td><%= movies2.actor.name %></td>
				<td><img src="<%= movies2.poster %>" height="150" width="100"></td>
				<td><%= movies2.rating %></td>
        <td><%= link_to 'Show', movies2 %></td>
        <td><%= link_to 'Edit', edit_movies2_path(movies2) %></td>
        <td><%= link_to 'Destroy', movies2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Movies2', new_movies2_path %>
